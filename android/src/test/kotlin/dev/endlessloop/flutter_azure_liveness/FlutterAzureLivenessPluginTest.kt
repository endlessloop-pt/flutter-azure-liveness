package dev.endlessloop.flutter_azure_liveness

// Android-side unit tests are intentionally minimal — integration testing
// of the liveness UI is performed on a real device/emulator with the Azure SDK.
// Dart-layer tests (mock MethodChannel) live in test/flutter_azure_liveness_test.dart.
